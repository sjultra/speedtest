# Default values for Speedtest chart.
# Update these values according to your requirements.

influxdb:
  image: influxdb:1.8
  adminUser: admin
  adminPassword: password
  db: speedtest

grafana:
  image: grafana/grafana:6.7.2
  adminPassword: admin
  port: 3000

speedtest:
  image: sjultra/speedtest-grafana:latest
  interval: 60
  host: ""

